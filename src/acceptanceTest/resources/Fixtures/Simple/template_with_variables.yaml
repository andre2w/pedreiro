---
- type: folder
  name: "{{ project_name }}"
  children:
    - type: file
      name: build.gradle
      content: |-
        plugins {
          id 'org.jetbrains.kotlin.jvm' version '1.3.71'
        }
        group '{{ package_name }}'
        version '1.0-SNAPSHOT'